# flix-scheduler-bot

Test project aimed primarily at learning [Flix](https://flix.dev/) — an effectful, functional-first language on the JVM platform.

Planned as a Telegram bot that keeps a list of current tasks. All communication with the Telegram API happens in Flix via the `Http` effect.
